// Create a do-while loop that runs at least once and prints "Learning C" even if the condition is false. 
#include<stdio.h>

int main() {
    int condition = 0;
    do {
        printf("Learning C\n");
    } while (condition);
    return 0;
}