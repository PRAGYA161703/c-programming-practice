// Write a C program to find the length of the string "Programming" using the strlen() function. 
#include <stdio.h> 
#include <string.h> 
 
int main() { 
    char name[] = "Programming"; 
    printf("Length = %ld\n", strlen(name));   
    return 0; 
}